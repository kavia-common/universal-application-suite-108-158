{
  "name": "frontend_react_js-reset",
  "version": "0.0.0",
  "private": true,
  "description": "Placeholder package.json to satisfy CI after full reset. No app scaffold included.",
  "scripts": {
    "lint": "echo \"No lintable sources after reset\"",
    "build": "echo \"No build steps after reset\"",
    "test": "echo \"No tests after reset\""
  }
}
